{
  "collectionName": "components_productos_stock_colores",
  "info": {
    "displayName": "Stock por Color",
    "description": "Cantidad de stock por color específico con detalle opcional de tallas"
  },
  "options": {},
  "attributes": {
    "color": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::color.color",
      "required": true
    },
    "cantidad": {
      "type": "integer",
      "required": false,
      "min": 0,
      "description": "Cantidad total del color. Si hay tallas, se calcula automáticamente"
    },
    "stock_por_tallas": {
      "type": "component",
      "repeatable": true,
      "component": "productos.stock-por-tallas",
      "required": false
    }
  }
}